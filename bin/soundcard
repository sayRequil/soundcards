#!/usr/bin/env python3

from soundcard import soundcard as sc
import soundcard.argparser as ap

parser = ap.parser()

args = parser.getArgs
if args[0] == "build":
  sc(build=True)
  
  
#
# Command:
# soundcard "build"
#
